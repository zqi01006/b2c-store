<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zqi.order.mapper.OrderMapper">
    <select id="selectAdminOrder" resultType="com.zqi.vo.AdminOrderVo">
        SELECT order_id,user_id,order_time,COUNT(product_id) product_num
            ,SUM(product_num) order_num,SUM(product_price) order_price
                from orders Group BY order_id LIMIT #{offset},#{pageSize};
    </select>
</mapper>